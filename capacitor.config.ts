import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tetris.app',
  appName: 'Cross Platform App',
  webDir: 'dist/tetris',
  bundledWebRuntime: false
};

export default config;
